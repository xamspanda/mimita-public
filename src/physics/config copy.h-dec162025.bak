// C:\important\go away v5\s\mimita-v5\src\physics\config.h
// the physics.json file at 
// C:\important\quiet\n\mimita-public\mimita-public\config\physics.json
// this has hot reload values that i think are more accurate

#pragma once

struct PhysicsConfig {
    float gravity;
    float moveSpeed;
    float jumpStrength;
};

inline PhysicsConfig PHYS = {
    -25.0f, // gravity
    20.0f, // move speed
    12.0f // jump strength 
};

// THESE MUST NOT BE constexpr
inline float PLAYER_HEIGHT = 1.8f;   // meters
inline float PLAYER_RADIUS = 0.35f;  // meters
inline float PLAYER_CAPSULE_HALF = (PLAYER_HEIGHT * 0.5f) - PLAYER_RADIUS;

// cs 1.6 movement attempt dec 16 2025 
// inline glm::vec3 PLAYER_HALF = glm::vec3(0.35f, 0.9f, 0.35f); // half width, half height, half depth

